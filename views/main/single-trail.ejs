<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <title><%= trail.name %>-Online Shopper</title>
  </head>
  <body>
    <% include ../partials/navbar %>
    <div class="container single-trail">
      <hr class="colorgraph single-hr" />
      <div class="row">
        <div class="col-md-6 col-xs-6">
          <div class="thumbnail">
            <img
              style="height: 20em; width: 27em;"
              src="<%= trail.image %>"
              alt="..."
              class="img-responsive"
            />
          </div>
          <div class="caption">
            <h3><%= trail.name %></h3>
            <p><%= trail.description %></p>
          </div>
        </div>
        <div
          class="col-md-4 col-xs-6 price"
          style="padding-top: 100px; margin: 0 auto;"
        >
          <h3>
            Price Per Item: <label for="price">$<%= trail.price %> </label>
          </h3>
          <form method="post" action="/api/fav/<%= trail._id %>" aria-label="...">
            <div class="btn-group" role="group">
              <input type="hidden" name="quantity" id="quantity" value="1" />
              <button
                class="item-count"
                type="button"
                id="minus"
                class="btn"
              >
                -
              </button>
              <button
                class="item-count"
                type="button"
                id="total"
                class="btn"
              >
                1
              </button>
              <button
                class="item-count"
                type="button"
                id="plus"
                class="btn"
              >
                +
              </button>
            </div>
            <div class="form-group">
              <input
                type="hidden"
                name="trail_id"
                id="trail"
                value="<%=trail._id%>"
              />
              <input
                type="hidden"
                name="priceHidden"
                id="priceHidden"
                value="<%=trail.price%>"
              />
              <input
                style="font-weight:400;"
                type="text"
                name="item"
                class="form-control"
                value="<%=trail.name%>"
                readonly
              />
              <input
                style="font-weight:400;"
                type="text"
                name="priceValue"
                id="priceValue"
                class="form-control"
                value="<%=trail.price%>"
                readonly
              />
            </div>
            <%if (user){%>
            <button type="submit" class="btn btn-primary btn-lg">
              Add to Favorites
            </button>
            <%} else {%>
            <a href="/api/users/register" class="btn btn-warning btn-lg"
              >Signup to Begin Buying</a
            >
            <%} %>
          </form>
        </div>
      </div>
      <hr style="margin-top: 40px;" class="colorgraph single-hr" />
    </div>
    <% include ../partials/scripts %>
  </body>
</html>
